<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8FAFC">

    <!-- Logo card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/logoCard"
        android:layout_width="96dp"
        android:layout_height="96dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="#FFFFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.30"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_traffic_logo"
            android:contentDescription="Logo giao thông" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Title line -->
    <LinearLayout
        android:id="@+id/layoutTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/logoCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="22dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Giao Thông"
            android:textColor="#111827"
            android:textSize="24sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" Thông Minh"
            android:textColor="#22C1E8"
            android:textSize="24sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Học luật dễ dàng, lái xe an toàn"
        android:textColor="#94A3B8"
        android:textSize="13sp"
        app:layout_constraintTop_toBottomOf="@id/layoutTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp" />

    <!-- Loading row -->
    <TextView
        android:id="@+id/tvLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Đang khởi tạo ứng dụng..."
        android:textColor="#94A3B8"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="46dp"
        android:layout_marginStart="42dp" />

    <TextView
        android:id="@+id/tvPercent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="75%"
        android:textColor="#22C1E8"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBaseline_toBaselineOf="@id/tvLoading"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="42dp" />

    <!-- Progress background -->
    <View
        android:id="@+id/progressBg"
        android:layout_width="0dp"
        android:layout_height="8dp"
        android:background="@drawable/bg_splash_progress_track"
        app:layout_constraintTop_toBottomOf="@id/tvLoading"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="10dp"
        android:layout_marginStart="42dp"
        android:layout_marginEnd="42dp" />

    <!-- Progress value -->
    <View
        android:id="@+id/progressValue"
        android:layout_width="0dp"
        android:layout_height="8dp"
        android:background="@drawable/bg_splash_progress_fill"
        app:layout_constraintTop_toTopOf="@id/progressBg"
        app:layout_constraintBottom_toBottomOf="@id/progressBg"
        app:layout_constraintStart_toStartOf="@id/progressBg"
        app:layout_constraintEnd_toStartOf="@id/guideProgress" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.74" />

    <!-- Bottom version -->
    <TextView
        android:id="@+id/tvVersion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PHIÊN BẢN 2.1.0"
        android:textColor="#CBD5E1"
        android:textSize="10sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="18dp" />

</androidx.constraintlayout.widget.ConstraintLayout>